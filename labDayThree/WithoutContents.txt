#include <iostream>
#include <fstream>
using namespace std;

int main()
{
  ifstream in;
  ofstream out;
  cout << "Testing : " << 162 << " = " << 4*2 << ".\n\n";
  out.open("WithoutContents.txt");
  in.open("ex2.cpp");
  char content;
  while (!in.eof())
    {
      in.get(content);
      if(content == '') *') && (in.get() != '')){;}
	}
      out.put(content);
    }
  in.close();
  out.close();
  return(0);

}

